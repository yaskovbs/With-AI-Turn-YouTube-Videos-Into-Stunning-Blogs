// This file defines global types for the application.
export type AppState = 'loading' | 'idle' | 'error';
// Explicitly mark this file as a module in the most minimal way possible
// to resolve 'Unexpected token ;' errors in certain module environments.
export {};